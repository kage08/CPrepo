#include <bits/stdc++.h>
using namespace std;

typedef unsigned long ul;
typedef unsigned long long ull;
typedef long long ll;

int main(){
	ul N,K;
	cin>>N>>K;
	vector<ul> candy(N);
	for(ul i=0;i<N;i++)
		cin>>candy[i];
	vector<vector<ull> >soln(K+1,vector<ull>(N+1,0));

	
}
